{
  "name": "leave1hourinadvance-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "backend",
    "runtime-executor"
  ],
  "scripts": {
    "lint": "echo \"(placeholder) add workspace lint\"",
    "test": "echo \"(placeholder) add workspace test\"",
    "build": "echo \"(placeholder) add workspace build\""
  }
}

