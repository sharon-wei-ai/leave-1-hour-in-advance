# 安全基线（MVP）
- 零原文件存储：上传后仅内存/临时处理，完成即清理，不落盘。
- 运行时安全：容器非 root，默认只读文件系统 + 临时目录；网络外网白名单。
- 资源限制：CPU/内存/超时/并发需配置；超限强制终止。
- 日志脱敏：过滤敏感信息（密钥、Token、PII），统一错误格式。
- 密钥管理：不提交真实密钥；使用环境变量/密钥管理服务；支持轮换。
- 审计与监控：记录关键操作（发布/修改/下架/调用）；运行时健康与失败告警。

