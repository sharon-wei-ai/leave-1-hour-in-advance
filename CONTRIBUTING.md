# 贡献与分支/版本规范（MVP 占位）

- 分支策略：main 受保护，开发在 feature 分支；PR 必须通过 CI 并经代码评审。
- 提交信息：建议遵循 `<type>: <desc>`（如 feat/fix/chore/docs）。
- 版本策略：功能发布=固化，需版本号推进（语义化版本优先，MVP 阶段可简化）。
- CI 要求：lint/test/build 需通过；Docker 构建占位，后续接入镜像推送/扫描。

